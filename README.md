# ExitPass

Bootstrapped multi-service .NET 8 solution baseline for ExitPass.

## Important note
This repository was generated in an environment without the .NET SDK, so the files were not compile-verified here.
Run `dotnet restore` and `dotnet build` on your machine and then fix any package/version drift.
