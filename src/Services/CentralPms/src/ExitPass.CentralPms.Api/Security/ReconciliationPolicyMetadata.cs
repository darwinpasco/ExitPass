namespace ExitPass.CentralPms.Api.Security;

/// <summary>
/// Placeholder metadata for future reconciliation RBAC policy enforcement.
/// </summary>
public sealed record ReconciliationPolicyMetadata(string PolicyName);
